package LacksDataBaseProject;

import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class Login {
    static Scanner input = new Scanner(System.in);
    static List<User> listOfUsersLogged = new ArrayList<>();

/*    static void login() {
        System.out.println("LOGIN");

        System.out.print("Login: ");
        User.login = input.next();
        System.out.print("Password: ");
        User.password = input.next();

        for (User user : User.listOfUsers) {
        if (user.getLogin().equals(User.login) {
            if (user.getPassword().equals(User.password)) {
                listOfUsersLogged.add(user);
                System.out.println(listOfUsersLogged);
            } else {
                System.out.println("User login or password is not correct.");
            }
        }
    }

    static void logout(){

        }*/

}
